name: Unified Coverage & Quality Analysis

on:
    workflow_dispatch: # Manual trigger for testing

jobs:
    aggregate:
        name: Aggregate & Analyze
        runs-on: ubuntu-latest

        steps:
            # ==========================================
            # SETUP
            # ==========================================
            - name: Checkout Parent Repo with Submodules
              uses: actions/checkout@v4
              with:
                  submodules: recursive
                  fetch-depth: 0
                  token: ${{ secrets.GH_PAT }}

            - name: Set up JDK 21
              uses: actions/setup-java@v4
              with:
                  java-version: "21"
                  distribution: "temurin"

            - name: Set up Python 3.11
              uses: actions/setup-python@v5
              with:
                  python-version: "3.11"

            # ==========================================
            # DOWNLOAD COVERAGE ARTIFACTS (Throttling Fix)
            # ==========================================
            - name: Download Android Coverage
              uses: actions/download-artifact@v4
              continue-on-error: true
              with:
                  name: android-coverage-reports
                  path: BinItRightMobileApp/app/build/reports/jacoco
                  repository: ysriser/BinItRightMobileApp
                  github-token: ${{ secrets.GH_PAT }}

            - name: Wait for API cooldown
              run: sleep 5

            - name: Download Java Coverage
              uses: actions/download-artifact@v4
              continue-on-error: true
              with:
                  name: java-coverage-reports
                  path: BinItRightWebApp/target/site/jacoco
                  repository: ysriser/BinItRightWebApp
                  github-token: ${{ secrets.GH_PAT }}

            - name: Wait for API cooldown
              run: sleep 5

            - name: Download Python Coverage
              uses: actions/download-artifact@v4
              continue-on-error: true
              with:
                  name: python-coverage-reports
                  path: BinItRightML
                  repository: ysriser/BinItRightML
                  github-token: ${{ secrets.GH_PAT }}

            # ==========================================
            # SONARCLOUD ANALYSIS
            # ==========================================
            - name: Cache SonarCloud Packages
              uses: actions/cache@v4
              with:
                  path: ~/.sonar/cache
                  key: ${{ runner.os }}-sonar-${{ hashFiles('**/pom.xml', '**/build.gradle.kts') }}
                  restore-keys: ${{ runner.os }}-sonar

            - name: List Submodule Files (Debug)
              run: |
                  echo "Checking Mobile App Submodule Path:"
                  ls -R BinItRightMobileApp/app/src/test || echo "❌ Path not found!"

            - name: Compile Java Web App
              run: |
                  if [ -d "BinItRightWebApp" ]; then
                     cd BinItRightWebApp
                     chmod +x mvnw
                     ./mvnw clean compile test-compile dependency:copy-dependencies -DskipTests
                  else
                     echo "❌ BinItRightWebApp directory not found!"
                     exit 1
                  fi

            - name: Run SonarCloud Analysis
              uses: SonarSource/sonarcloud-github-action@master
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
              with:
                  args: >
                      -Dsonar.verbose=true
                      -Dsonar.projectKey=ysriser_BinItRightOrchestrator
                      -Dsonar.organization=ysriser

            # ==========================================
            # RESULTS SUMMARY
            # ==========================================
            - name: Generate Summary
              if: always()
              run: |
                  echo "## 📊 Unified Coverage Analysis Complete" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "✅ **SonarCloud Dashboard:** https://sonarcloud.io/project/overview?id=rselvimaha_BinItRightParent" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "### Coverage Sources:" >> $GITHUB_STEP_SUMMARY
                  echo "- 📱 Android Mobile App" >> $GITHUB_STEP_SUMMARY
                  echo "- ☕ Java Spring Boot Web" >> $GITHUB_STEP_SUMMARY
                  echo "- 🐍 Python ML Service" >> $GITHUB_STEP_SUMMARY
